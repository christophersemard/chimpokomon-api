<form method="post" action="{{ path('app_chimpokofood_delete', {'id': chimpokofood.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chimpokofood.id) }}">
    <button class="btn">Delete</button>
</form>
