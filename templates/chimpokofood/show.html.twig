{% extends 'base.html.twig' %}

{% block title %}Chimpokofood{% endblock %}

{% block body %}
    <h1>Chimpokofood</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chimpokofood.id }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ chimpokofood.status }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ chimpokofood.name }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ chimpokofood.amount }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_chimpokofood_index') }}">back to list</a>

    <a href="{{ path('app_chimpokofood_edit', {'id': chimpokofood.id}) }}">edit</a>

    {{ include('chimpokofood/_delete_form.html.twig') }}
{% endblock %}
